@model List<DietMealApp.Core.DTO.Products.ProductDTO>
@using DietMealApp.Core.Enums;
@using DietMealApp.Core.Extensions;
@{
    ViewData["Title"] = "Lista produktów";
}
<h1>Produkty</h1>

<form method="get">
    <div class="input-group col-sm-10">
        <input id="searchBox" class="form-control" /> @*search box do uzupełnienia*@
        <div class="input-group-append">
            <button type="submit" class="btn btn-primary">Szukaj</button>
        </div>
    </div>
</form>

<p></p>
<div class="form-group row">
    <div class="col-sm-10">
        <a asp-controller="Product" asp-action="Create" class="btn btn-primary">Dodaj nowy</a>
    </div>
</div>

  <div class="row row-cols-1 row-cols-md-4 g-2">
    @foreach (var item in Model)
    {
        <div class="col">
        <div class="card">
            <img src="@item.PhotoPath" class="card-img-top">
            <div class="card-body">
                <h5 class="card-title"> <a asp-controller="Product" asp-action="Edit" asp-route-id="@item.Id">@item.ProductName</a></h5>
                <p class="card-text">@item.Category.GetDisplayName()</p>
                <p class="card-text">@item.Kcal kcal w @item.QuantityUnit @item.Unit </p>
            </div>
            <div class="card-footer">
                <a asp-controller="Product" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger m-1">Usuń</a>
            </div>
        </div>
        </div>
    }
      </div>
@*
    // SKRYPT DO WYKORZYSTANIA JEŻELI WYSZUKIWANIE MIAŁOBY BYĆ WYWOŁYWANE PRZEZ AJAX - DO POPRAWY ODŚWIEŻANIE TABELI //

    <script type="text/javascript">
    var searchRequest = null;

    $(function () {
    var minlength = 3;

    $("#searchBox").keyup(function () {
    var that = this,
    value = $(this).val();
    if (value.length >= minlength ) {
    if (searchRequest != null)
    searchRequest.abort();
    searchRequest = $.ajax({
    type: "GET",
    url: "/Products/",
    data: {
    'SearchTerm' : value
    },
    dataType: "text",
    success: function(msg){
    //we need to check if the value is the same
    if (value == $(that).val()) {
    console.log(data);
    //Receiving the result of search here
    }
    }
    });
    }
    });
    });
    </script>*@
