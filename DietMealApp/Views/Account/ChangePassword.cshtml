@using DietMealApp.Core.ViewModels.Identity
@model ChangePasswordViewModel
@{
    ViewData["Title"] = "Zmiana hasła";
}
@if (!ViewData.ModelState.IsValid && ViewData.ModelState["Password"].Errors != null)
{
    @if (ViewData.ModelState["Password"].Errors.Count > 0)
    {
        <div class="alert alert-danger" role="alert">
            <strong>Błąd! </strong>@ViewData.ModelState["Password"].Errors.First().ErrorMessage
        </div>
    }
}
@if (ViewData.ModelState.IsValid && ViewData["Success"] != null)
{
    <div class="alert alert-success" role="alert">
        @ViewData["Success"]
    </div>
}
<h1>@ViewData["Title"]</h1>
<div class="row">
    <div class="col-md-4">
        <form method=post>
            <section>
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="CurrentPassword"></label>
                    <input asp-for="CurrentPassword" class="form-control" />
                    <span asp-validation-for="CurrentPassword" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="NewPassword"></label>
                    <input asp-for="NewPassword" class="form-control" />
                    <span asp-validation-for="NewPassword" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="ConfirmPassword"></label>
                    <input asp-for="ConfirmPassword" class="form-control" />
                    <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <button type="submit" id="btnChangePassword" name="ChangePassword" class="btn btn-primary">Zmień hasło</button>
                </div>
            </section>
        </form>
    </div>
</div>

<script>
        $(document).ready(function() {
    $(".alert").fadeTo(2000, 500).slideUp(500, function(){
        $(".alert").slideUp(500);
    });
    });
            //$(document).ready(function(){
            //    $("#btnChangePassword").click(function(){
            //        ChangePassword();
            //    });
            //});
            //function ChangePassword(){
            //    var currentPassword = $("#CurrentPassword").val();
            //    var newPassword = $("#NewPassword").val();
            //    var confirmPassword = $("#ConfirmPassword").val();

            //    var objViewModel = {CurrentPassword: currentPassword, NewPassword: newPassword, ConfirmPassword: confirmPassword};
            //    console.log(objViewModel);
            //    $.ajax({
            //        async: true,
            //        type: 'POST',
            //        dataType: 'JSON',
            //        contentType: 'application/json',
            //        data: JSON.stringify({CurrentPassword: currentPassword, NewPassword: newPassword, ConfirmPassword: confirmPassword}),
            //        url: 'ChangePassword',
            //        success: function(response){
            //            if(response.success === true){
            //                toastr.success(response.message, 'Success Alert', new {timeOut: 3000});
            //            } else{
            //                toastr.info(response.message, 'Information Alert', new {timeOut: 3000});
            //            };
            //        },
            //        error: function(){
            //            toastr.error('There is some problem to process your request.', 'Error Alert', new {timeOut: 3000});
            //        }
            //    });
            //}
</script>